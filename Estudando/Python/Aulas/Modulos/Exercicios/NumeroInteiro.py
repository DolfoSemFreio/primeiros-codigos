import math # para importar só uma funcao basta escrever 'from math import trunc' dai n precisa usar o math.trunc
numb = float(input('Digite um numero real: '))
print(f'O valor {numb} quando quebrado fica {math.trunc(numb)}') #tem jeito mais facil, porem estou aprendendo sobre import agr