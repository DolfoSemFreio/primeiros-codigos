function soma(n1=0, n2=0) { // use a virgula para colocar mais de 1 elemento
    return n1 + n2 // dps do return n precisa colocar nada além do que ira fazer como os valores do elemento 
} console.log(soma(80, 1))